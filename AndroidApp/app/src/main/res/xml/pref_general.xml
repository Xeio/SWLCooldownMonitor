<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <EditTextPreference
        android:hint="@string/character_names_hint"
        android:capitalize="words"
        android:inputType="textMultiLine"
        android:key="pref_character_name"
        android:maxLines="10"
        android:selectAllOnFocus="false"
        android:singleLine="false"
        android:title="@string/character_names" />

    <SwitchPreference
        android:defaultValue="true"
        android:key="pref_patron"
        android:summary="@string/pref_description_patron"
        android:title="@string/pref_title_patron" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog t,eo
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <!--<ListPreference-->
        <!--android:defaultValue="-1"-->
        <!--android:entries="@array/pref_example_list_titles"-->
        <!--android:entryValues="@array/pref_example_list_values"-->
        <!--android:key="example_list"-->
        <!--android:negativeButtonText="@null"-->
        <!--android:positiveButtonText="@null"-->
        <!--android:title="@string/pref_title_add_friends_to_messages" />-->

</PreferenceScreen>
